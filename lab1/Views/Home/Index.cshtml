@{
    ViewData["Title"] = "Головна";
}

<style>
    /* TYPOGRAPHY */
    .hero-section {
        text-align: center;
        padding: 80px 0 100px 0;
        animation: fadeUp 1s cubic-bezier(0.2, 0.8, 0.2, 1);
    }

    .display-title {
        font-size: 4rem; /* Великий, впевнений шрифт */
        font-weight: 600;
        letter-spacing: -0.03em;
        line-height: 1.1;
        background: linear-gradient(180deg, #fff 0%, rgba(255,255,255,0.7) 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        margin-bottom: 24px;
    }

    .subtitle {
        font-size: 1.25rem;
        color: var(--text-secondary);
        max-width: 500px;
        margin: 0 auto 40px auto;
        font-weight: 400;
        letter-spacing: -0.01em;
    }

    /* BUTTONS - Tactile Light */
    .btn-group-custom {
        display: inline-flex;
        gap: 16px;
    }

    .btn-apple-primary {
        background: #fff;
        color: #000;
        padding: 14px 32px;
        border-radius: 100px;
        font-weight: 600;
        font-size: 1rem;
        text-decoration: none;
        transition: transform 0.2s cubic-bezier(0.25, 1, 0.5, 1), box-shadow 0.2s ease;
        border: none;
    }

        .btn-apple-primary:hover {
            transform: scale(1.04);
            box-shadow: 0 0 20px rgba(255, 255, 255, 0.3);
            color: #000;
        }

    .btn-apple-secondary {
        background: rgba(255, 255, 255, 0.1);
        color: #fff;
        padding: 14px 32px;
        border-radius: 100px;
        font-weight: 500;
        font-size: 1rem;
        text-decoration: none;
        backdrop-filter: blur(10px);
        transition: background 0.2s ease;
    }

        .btn-apple-secondary:hover {
            background: rgba(255, 255, 255, 0.2);
            color: #fff;
        }

    /* BENTO GRID CARDS */
    .bento-grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 24px;
        padding: 0 20px;
    }

    .glass-card {
        background: var(--glass-surface);
        border: 1px solid var(--glass-border);
        border-radius: 24px;
        padding: 40px;
        position: relative;
        overflow: hidden;
        transition: all 0.4s cubic-bezier(0.25, 0.8, 0.25, 1);
        display: flex;
        flex-direction: column;
        justify-content: space-between;
        height: 300px;
    }

        /* Ефект світіння зсередини при наведенні */
        .glass-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: radial-gradient(800px circle at var(--mouse-x) var(--mouse-y), rgba(255,255,255,0.06), transparent 40%);
            opacity: 0;
            transition: opacity 0.4s ease;
            pointer-events: none;
        }

        .glass-card:hover {
            transform: translateY(-5px) scale(1.01);
            background: rgba(255, 255, 255, 0.05);
            border-color: var(--glass-highlight);
            box-shadow: 0 30px 60px rgba(0,0,0,0.5);
        }

            .glass-card:hover::before {
                opacity: 1;
            }

    .card-icon {
        font-size: 2rem;
        margin-bottom: auto;
        opacity: 0.8;
    }

    .card-title {
        font-size: 1.5rem;
        font-weight: 600;
        color: #fff;
        margin-bottom: 8px;
    }

    .card-desc {
        color: var(--text-secondary);
        font-size: 0.95rem;
        line-height: 1.5;
    }

    /* Анімація появи */
    @@keyframes fadeUp {
        from {
            opacity: 0;
            transform: translateY(40px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    /* Адаптивність */
    @@media (max-width: 768px) {
        .bento-grid {
            grid-template-columns: 1fr;
        }

        .display-title {
            font-size: 3rem;
        }
    }
</style>

<div class="hero-section">
    <h1 class="display-title">Think different</h1>
    <p class="subtitle">
        IJournal - more than just a Journal <br />
        you, your ideas, and a boundless canvas.
    </p>

    <div class="btn-group-custom">
        <a asp-controller="Articles" asp-action="Index" class="btn-apple-primary">Читати</a>
        <a asp-controller="Articles" asp-action="Create" class="btn-apple-secondary">Написати</a>
    </div>
</div>

<div class="bento-grid">
    <div class="glass-card" onmousemove="handleMouseMove(event)">
        <div class="card-icon">⚡️</div>
        <div>
            <h3 class="card-title">Миттєвість</h3>
            <p class="card-desc">Інтерфейс, що реагує швидше за думку. Ми прибрали все зайве, залишивши тільки швидкість.</p>
        </div>
    </div>

    <div class="glass-card" onmousemove="handleMouseMove(event)">
        <div class="card-icon">💎</div>
        <div>
            <h3 class="card-title">Прозорість</h3>
            <p class="card-desc">Контекст завжди з вами. Наш "розумний матеріал" адаптується під ваш контент.</p>
        </div>
    </div>

    <div class="glass-card" onmousemove="handleMouseMove(event)">
        <div class="card-icon">🔒</div>
        <div>
            <h3 class="card-title">Приватність</h3>
            <p class="card-desc">Ваші дані належать вам. Надійна архітектура без компромісів.</p>
        </div>
    </div>
</div>

<script>
    // Скрипт для ефекту "слідування миші" на картках (Apple-style hover)
    function handleMouseMove(e) {
        const card = e.currentTarget;
        const rect = card.getBoundingClientRect();
        const x = e.clientX - rect.left;
        const y = e.clientY - rect.top;

        card.style.setProperty('--mouse-x', `${x}px`);
        card.style.setProperty('--mouse-y', `${y}px`);
    }
</script>