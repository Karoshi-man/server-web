@page
@model RegisterModel
@{
    ViewData["Title"] = "Sign Up";
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - iJournal</title>
    <style>
        /* --- Reset & Base --- */
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            font-family: -apple-system, BlinkMacSystemFont, "SF Pro Display", "SF Pro Text", "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
        }

        body {
            background-color: #010612;
            color: #ffffff;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            overflow: hidden;
            background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.8' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)' opacity='0.04'/%3E%3C/svg%3E");
        }

        /* --- Abyssal Background Ambient --- */
        .abyss-ambient {
            position: fixed;
            top: 0;
            left: 0;
            width: 100vw;
            height: 100vh;
            z-index: 0;
            pointer-events: none;
        }

        .deep-blob {
            position: absolute;
            border-radius: 50%;
            filter: blur(140px);
            opacity: 0.5;
            mix-blend-mode: screen;
            animation: deepFloat 25s infinite alternate ease-in-out;
        }

        .db-1 {
            width: 70vw;
            height: 70vw;
            background: linear-gradient(to right, #020024, #090979);
            top: -20%;
            left: -20%;
        }

        .db-2 {
            width: 50vw;
            height: 50vw;
            background: linear-gradient(to right, #110022, #001275);
            bottom: -20%;
            right: -10%;
            animation-direction: alternate-reverse;
            animation-duration: 35s;
        }

        @@keyframes deepFloat {
            0% {
                transform: translate(0, 0) scale(1) rotate(0deg);
            }

            100% {
                transform: translate(5vw, 10vh) scale(1.05) rotate(15deg);
            }
        }

        /* --- Hyper-Glass Split Container --- */
        .hyper-glass-container {
            position: relative;
            z-index: 1;
            display: flex;
            width: 90%;
            max-width: 1200px;
            min-height: 700px;
            background: rgba(10, 20, 40, 0.25);
            backdrop-filter: blur(60px) saturate(120%);
            -webkit-backdrop-filter: blur(60px) saturate(120%);
            border: 1px solid rgba(255, 255, 255, 0.08);
            border-radius: 40px;
            box-shadow: inset 2px 2px 4px rgba(255, 255, 255, 0.15), inset -2px -2px 10px rgba(0, 0, 0, 0.5), 0 40px 100px rgba(0, 0, 0, 0.8);
            overflow: hidden;
        }

        /* Ліва частина - Контент */
        .glass-content {
            flex: 1;
            padding: 60px;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            background: rgba(0, 0, 0, 0.2);
            border-right: 1px solid rgba(255, 255, 255, 0.05);
        }

        .brand-logo {
            font-size: 24px;
            font-weight: 700;
            letter-spacing: -0.03em;
            display: flex;
            align-items: center;
            gap: 10px;
            text-decoration: none;
            color: #fff;
        }

            .brand-logo svg {
                width: 24px;
                height: 24px;
                fill: #fff;
            }

        .hero-title {
            font-size: 56px;
            font-weight: 800;
            letter-spacing: -0.03em;
            line-height: 1.2;
            padding-bottom: 10px;
            margin-bottom: 10px;
            background: linear-gradient(180deg, #ffffff 40%, #7b88a8 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .hero-subtitle {
            font-size: 18px;
            color: rgba(255, 255, 255, 0.6);
            line-height: 1.6;
            max-width: 90%;
            text-align: center;
            margin: 0 auto;
        }

        .abstract-quote {
            font-size: 14px;
            color: rgba(255, 255, 255, 0.4);
            border-left: 2px solid rgba(0, 113, 227, 0.5);
            padding-left: 15px;
        }

        /* Права частина - Форма */
        .glass-form-wrapper {
            flex: 1;
            padding: 60px;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        .form-header {
            margin-bottom: 40px;
        }

            .form-header h2 {
                font-size: 28px;
                font-weight: 600;
                margin-bottom: 8px;
            }

            .form-header p {
                color: rgba(255, 255, 255, 0.5);
                font-size: 15px;
            }

        .input-group {
            margin-bottom: 24px;
            position: relative;
        }

        .deep-input {
            width: 100%;
            padding: 16px 20px;
            background: rgba(0, 0, 0, 0.3);
            border: 1px solid rgba(255, 255, 255, 0.08);
            border-radius: 16px;
            color: #fff;
            font-size: 16px;
            transition: all 0.3s ease;
            box-shadow: inset 0 2px 6px rgba(0,0,0,0.5);
        }

            .deep-input::placeholder {
                color: rgba(255, 255, 255, 0.3);
            }

            .deep-input:focus {
                outline: none;
                background: rgba(10, 20, 40, 0.5);
                border-color: rgba(0, 113, 227, 0.5);
                box-shadow: inset 0 2px 6px rgba(0,0,0,0.5), 0 0 0 4px rgba(0, 113, 227, 0.15);
            }

        .btn-accent-glass {
            display: block;
            width: 220px;
            margin: 20px auto 0 auto;
            padding: 16px;
            border-radius: 14px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
            background: rgba(255, 255, 255, 0.05);
            color: #ffffff;
            border: 1px solid rgba(255, 255, 255, 0.15);
            backdrop-filter: blur(20px);
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.3), inset 0 1px 2px rgba(255, 255, 255, 0.1);
        }

            .btn-accent-glass:hover:not(:disabled) {
                transform: translateY(-2px);
                background: rgba(255, 255, 255, 0.1);
                border-color: rgba(255, 255, 255, 0.3);
                box-shadow: 0 8px 25px rgba(0, 0, 0, 0.4), inset 0 1px 2px rgba(255, 255, 255, 0.2);
            }

            /* Стан вимкненої кнопки */
            .btn-accent-glass:disabled {
                background: rgba(255, 255, 255, 0.02);
                color: rgba(255, 255, 255, 0.3);
                border-color: rgba(255, 255, 255, 0.05);
                cursor: not-allowed;
                box-shadow: none;
            }

        /* === КРАСИВІ ПОМИЛКИ (Скляна червона плашка) === */
        /* Приховуємо пустий блок, щоб він не займав місце */
        .validation-summary-valid {
            display: none !important;
        }

        /* Стиль для блоку, коли є помилка */
        .validation-summary-errors {
            background: rgba(255, 69, 58, 0.1);
            border: 1px solid rgba(255, 69, 58, 0.3);
            border-radius: 16px;
            padding: 16px 20px;
            margin-bottom: 24px;
            color: #ff453a;
            font-size: 14px;
            backdrop-filter: blur(10px);
            box-shadow: 0 4px 15px rgba(255, 69, 58, 0.05), inset 0 1px 2px rgba(255, 69, 58, 0.1);
            animation: shake 0.4s cubic-bezier(.36,.07,.19,.97) both;
        }

            .validation-summary-errors ul {
                list-style: none;
                padding: 0;
                margin: 0;
            }

            .validation-summary-errors li {
                margin-bottom: 6px;
                display: flex;
                align-items: flex-start;
                gap: 8px;
            }

                .validation-summary-errors li:last-child {
                    margin-bottom: 0;
                }

                .validation-summary-errors li::before {
                    content: "•";
                    color: #ff453a;
                    font-weight: bold;
                }

        @@keyframes shake {
            10%, 90% {
                transform: translate3d(-1px, 0, 0);
            }

            20%, 80% {
                transform: translate3d(2px, 0, 0);
            }

            30%, 50%, 70% {
                transform: translate3d(-4px, 0, 0);
            }

            40%, 60% {
                transform: translate3d(4px, 0, 0);
            }
        }
        /* ============================================== */

        /* Помилки під конкретним інпутом */
        .text-danger {
            color: #ff453a;
            font-size: 13px;
            margin-top: 8px;
            display: block;
            padding-left: 5px;
            font-weight: 500;
        }
   

        .login-link {
            text-align: center;
            margin-top: 30px;
            font-size: 15px;
            color: rgba(255, 255, 255, 0.5);
        }

            .login-link a {
                color: #ffffff;
                text-decoration: none;
                font-weight: 500;
                transition: opacity 0.2s;
                border-bottom: 1px solid transparent;
            }

                .login-link a:hover {
                    opacity: 0.8;
                    border-bottom: 1px solid #ffffff;
                }

        /* === СТИЛІ ДЛЯ ЧЕКЛІСТА ПАРОЛЯ === */
        .password-checklist {
            margin-top: 12px;
            padding-left: 5px;
            display: flex;
            flex-direction: column;
            gap: 8px;
            font-size: 13px;
        }

        .check-item {
            color: rgba(255, 255, 255, 0.4);
            display: flex;
            align-items: center;
            gap: 8px;
            transition: color 0.3s ease;
        }

            .check-item svg {
                width: 14px;
                height: 14px;
                fill: currentColor;
                transition: fill 0.3s ease;
            }

            .check-item.valid {
                color: #00ffc8; /* Неоновий зелений при успіху */
            }

        /* Адаптивність */
        @@media (max-width: 900px) {
            .hyper-glass-container {
                flex-direction: column;
                min-height: auto;
                margin: 20px;
            }

            .glass-content {
                padding: 40px;
                border-right: none;
                border-bottom: 1px solid rgba(255, 255, 255, 0.05);
            }

            .glass-form-wrapper {
                padding: 40px;
            }

            .hero-title {
                font-size: 40px;
            }

            .btn-accent-glass {
                width: 100%;
            }
        }
    </style>
</head>
<body>

    <div class="abyss-ambient">
        <div class="deep-blob db-1"></div>
        <div class="deep-blob db-2"></div>
    </div>

    <div class="hyper-glass-container">
        <div class="glass-content">
            <a href="~/" class="brand-logo">
                <svg viewBox="0 0 24 24"><path d="M12 2L2 22h20L12 2zm0 3.83L18.17 19H5.83L12 5.83z" /></svg>
                iJournal
            </a>

            <div style="margin-top: auto; margin-bottom: auto;">
                <h1 class="hero-title">Portray YourSelf</h1>
                <p class="hero-subtitle">Establish your secure access point. Your data is protected by enterprise-grade ASP.NET Core cryptography</p>
            </div>

            <div class="abstract-quote">
                "Security is not a feature. It’s a foundation"
            </div>
        </div>

        <div class="glass-form-wrapper">
            <div class="form-header">
                <h2>Create Account</h2>
                <p>Enter your details to generate your cryptographic key</p>
            </div>

            <form id="registerForm" asp-route-returnUrl="@Model.ReturnUrl" method="post">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                <div class="input-group">
                    <input asp-for="Input.Email" class="deep-input" aria-required="true" placeholder="Email" readonly onfocus="this.removeAttribute('readonly');" />
                    <span asp-validation-for="Input.Email" class="text-danger"></span>
                </div>

                <div class="input-group">
                    <input asp-for="Input.Password" class="deep-input" id="passwordInput" aria-required="true" placeholder="Password" type="password" readonly onfocus="this.removeAttribute('readonly');" />
                    <span asp-validation-for="Input.Password" class="text-danger"></span>

                    <div class="password-checklist">
                        <div class="check-item" id="check-length">
                            <svg viewBox="0 0 24 24"><path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z" /></svg>
                            At least 6 characters
                        </div>
                        <div class="check-item" id="check-letter">
                            <svg viewBox="0 0 24 24"><path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z" /></svg>
                            Contains a letter
                        </div>
                        <div class="check-item" id="check-number">
                            <svg viewBox="0 0 24 24"><path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z" /></svg>
                            Contains a number
                        </div>
                    </div>
                </div>

                <div class="input-group">
                    <input asp-for="Input.ConfirmPassword" class="deep-input" aria-required="true" placeholder="Confirm Password" type="password" readonly onfocus="this.removeAttribute('readonly');" />
                    <span asp-validation-for="Input.ConfirmPassword" class="text-danger"></span>
                </div>

                <button id="registerSubmit" type="submit" class="btn-accent-glass" disabled>Sign Up</button>
            </form>

            <div class="login-link">
                Already have an account? <a asp-page="./Login" asp-route-returnUrl="@Model.ReturnUrl">Sign In</a>
            </div>
        </div>
    </div>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/jquery-validation/dist/jquery.validate.min.js"></script>
    <script src="~/lib/jquery-validation-unobtrusive/jquery.validate.unobtrusive.min.js"></script>

    <script>
        document.addEventListener('DOMContentLoaded', function () {
            const passwordInput = document.getElementById('passwordInput');
            const btnSubmit = document.getElementById('registerSubmit');

            const checkLength = document.getElementById('check-length');
            const checkLetter = document.getElementById('check-letter');
            const checkNumber = document.getElementById('check-number');

            const hasLetter = /[a-zA-Z]/;
            const hasNumber = /[0-9]/;

            passwordInput.addEventListener('input', function () {
                const val = passwordInput.value;
                let isValid = true;

                if (val.length >= 6) {
                    checkLength.classList.add('valid');
                } else {
                    checkLength.classList.remove('valid');
                    isValid = false;
                }

                if (hasLetter.test(val)) {
                    checkLetter.classList.add('valid');
                } else {
                    checkLetter.classList.remove('valid');
                    isValid = false;
                }

                if (hasNumber.test(val)) {
                    checkNumber.classList.add('valid');
                } else {
                    checkNumber.classList.remove('valid');
                    isValid = false;
                }

                if (isValid) {
                    btnSubmit.removeAttribute('disabled');
                } else {
                    btnSubmit.setAttribute('disabled', 'disabled');
                }
            });
        });
    </script>
</body>
</html>